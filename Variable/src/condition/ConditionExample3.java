package condition;

public class ConditionExample3 {

	public static void main(String[] args) {
		Math.random(); // 0 < random < 1 결과값은 0~1사이의 소수값을 반환한다.
		int randomValue = (int) (Math.random() * 6) + 1;
		int num = "";
		switch (randomValue) {
		case 1 :
		num = 1	
		}
		System.out.println((int) (Math.random() * 6) + 1); // int는 소수점을 버리고 정수만 가지고 온다.

	}
}
